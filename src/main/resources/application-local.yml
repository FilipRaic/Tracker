spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: postgres

  mail:
    host: localhost
    port: 1026
    username: test
    password: test
    from: noreply@trackerplatform.com
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
            required: false

frontend:
  url: http://localhost:4200

scheduler:
  cron:
    daily-check: "0 * * * * ?"
